let sentence = "I am learning js";
let sub = "js";

if (sentence.includes(sub)) {
  console.log(`"${sub}" exists in the string.`);
} else {
  console.log(`"${sub}" does NOT exist in the string.`);
}